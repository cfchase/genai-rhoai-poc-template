apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: bootstrap
  namespace: openshift-gitops
spec:
  goTemplate: true
  generators:
  - list:
      elements:
      - cluster: in-cluster
        name: rhoai-install
        repoURL: https://github.com/cfchase/genai-rhoai-poc-template.git
        targetRevision: chase-dev
        path: common-utils/rhoai/install-operator
      - cluster: in-cluster
        name: rhoai-config
        repoURL: https://github.com/cfchase/genai-rhoai-poc-template.git
        targetRevision: chase-dev
        path: common-utils/rhoai/config
      - cluster: in-cluster
        name: rhoai-deploy
        repoURL: https://github.com/cfchase/genai-rhoai-poc-template.git
        targetRevision: chase-dev
        path: common-utils/rhoai/deploy
  template:
    metadata:
      name: "{{ .name }}"
      namespace: openshift-gitops
      labels:
        component: bootstrap
    spec:
      project: default
      source:
        repoURL: "{{ .repoURL }}"
        targetRevision: "{{ .targetRevision }}"
        path: "{{ .path }}"
      destination:
        name: "{{ .cluster }}"
      syncPolicy:
        syncOptions:
          - RespectIgnoreDifferences=true
          - Retry=true
        retry:
          limit: 50
          backoff:
            duration: 15s
            factor: 2
            maxDuration: 5m
  templatePatch: |
    spec:
      syncPolicy:
        automated:
          prune: {{ .prune | default "true" }}
          selfHeal: {{ .selfHeal | default "true" }}
